- name: install and run nginx
  hosts: web
  become: yes
  tasks:
  - name: install nginx
    ansible.builtin.package:
      name: nginx
      state: latest
  - name: run nginx
    ansible.builtin.service:
      name: ngnix
      state: started
      enabled: yes